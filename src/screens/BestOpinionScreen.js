import React from 'react';
import { Text, StyleSheet } from 'react-native';

const BestOpinionScreen = () => {
  <Text style={styles.text}>Best Opinion Screen</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30
  }
});

export default BestOpinionScreen;