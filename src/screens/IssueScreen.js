import React from 'react';
import { Text, StyleSheet } from 'react-native';

const IssueScreen = () => {
  <Text style={styles.text}>Issue Screen</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30
  }
});

export default IssueScreen;