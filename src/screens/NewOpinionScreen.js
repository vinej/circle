import React from 'react';
import { Text, StyleSheet } from 'react-native';

const NewOpinionScreen = () => {
  <Text style={styles.text}>New Opinion Screen</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30
  }
});

export default NewOpinionScreen;