import React from 'react';
import { Text, StyleSheet } from 'react-native';

const RuleScreen = () => {
  return (
    <Text style={styles.text}>Rule Screen</Text>
  );
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30
  }
});

export default RuleScreen;