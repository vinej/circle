import React from 'react';
import { Text, StyleSheet } from 'react-native';

const AllOpinionScreen = () => {
  <Text style={styles.text}>All Opinion Screen</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30
  }
});

export default AllOpinionScreen;