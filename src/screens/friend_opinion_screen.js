import React from 'react';
import { Text, StyleSheet } from 'react-native';

const FriendOpinionScreen = () => {
  <Text style={styles.text}>Friend Opinion Screen</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30
  }
});

export default FriendOpinionScreen;