import React from 'react';
import { Text, StyleSheet } from 'react-native';

const RappelScreen = () => {
  <Text style={styles.text}>Rappel Screen</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30
  }
});

export default RappelScreen;